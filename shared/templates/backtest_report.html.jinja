<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Backtest Report - {{ strategy_name }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .header .logo {
            max-width: 120px;
            max-height: 120px;
            margin-bottom: 15px;
            display: inline-block;
        }
        
        .header .brand-name {
            font-size: 1.8em;
            font-weight: bold;
            margin-bottom: 10px;
            letter-spacing: 2px;
            text-transform: uppercase;
            opacity: 0.95;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .header .subtitle {
            font-size: 1.1em;
            opacity: 0.9;
        }
        
        .content {
            padding: 40px;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        
        .info-card {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            border-left: 4px solid #667eea;
        }
        
        .info-card h3 {
            color: #667eea;
            font-size: 0.9em;
            text-transform: uppercase;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }
        
        .info-card .value {
            font-size: 1.5em;
            font-weight: bold;
            color: #333;
        }
        
        .info-card.profit {
            border-left-color: #28a745;
        }
        
        .info-card.profit .value {
            color: #28a745;
        }
        
        .info-card.loss {
            border-left-color: #dc3545;
        }
        
        .info-card.loss .value {
            color: #dc3545;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section h2 {
            color: #667eea;
            font-size: 1.8em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e9ecef;
        }
        
        .table-container {
            overflow-x: auto;
            margin-top: 20px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        th, td {
            padding: 12px 15px;
            text-align: left;
        }
        
        th {
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.85em;
            letter-spacing: 0.5px;
        }
        
        tbody tr {
            border-bottom: 1px solid #e9ecef;
        }
        
        tbody tr:hover {
            background: #f8f9fa;
        }
        
        tbody tr:last-child {
            border-bottom: none;
        }
        
        pre {
            background: #282c34;
            color: #abb2bf;
            padding: 20px;
            border-radius: 8px;
            overflow-x: auto;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 0.9em;
            line-height: 1.5;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
        
        .footer {
            background: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #6c757d;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            {% if logo_base64 != "" %}
            <img src="{{ logo_base64 }}" alt="WiseTrader Logo" class="logo" />
            {% endif %}
            <div class="brand-name">Wise Trader</div>
            <h1>📊 Backtest Report</h1>
            <div class="subtitle">{{ strategy_name }}</div>
        </div>
        
        <div class="content">
            <div class="section">
                <h2>📋 Strategy Information</h2>
                <div class="info-grid">
                    {% match user_fullname %}
                    {% when Some with (fullname) %}
                    <div class="info-card">
                        <h3>👤 User</h3>
                        <div class="value">{{ fullname }}</div>
                    </div>
                    {% when None %}
                    {% endmatch %}
                    <div class="info-card">
                        <h3>Strategy Name</h3>
                        <div class="value">{{ strategy_name }}</div>
                    </div>
                    <div class="info-card">
                        <h3>Exchange</h3>
                        <div class="value">{{ exchange }}</div>
                    </div>
                    <div class="info-card">
                        <h3>Pair</h3>
                        <div class="value">{{ pair }}</div>
                    </div>
                    <div class="info-card">
                        <h3>Timeframe</h3>
                        <div class="value">{{ timeframe }}</div>
                    </div>
                    <div class="info-card">
                        <h3>Time Range</h3>
                        <div class="value">{{ timerange }}</div>
                    </div>
                    <div class="info-card">
                        <h3>Created At</h3>
                        <div class="value">{{ created_at }}</div>
                    </div>
                </div>
            </div>
            
            <div class="section">
                <h2>📈 Key Metrics</h2>
                <div class="info-grid">
                    <div class="info-card">
                        <h3>Total Trades</h3>
                        <div class="value">{{ trades }}</div>
                    </div>
                    <div class="info-card {% if profit_pct >= 0.0 %}profit{% else %}loss{% endif %}">
                        <h3>Total Profit</h3>
                        <div class="value">{{ "{:.2}"|format(profit_pct) }}%</div>
                    </div>
                </div>
            </div>
            
            <div class="section">
                <h2>⏱️ Performance</h2>
                <div class="info-grid">
                    <div class="info-card">
                        <h3>Backtest Execution Time</h3>
                        <div class="value">{{ backtest_time_secs }}s</div>
                    </div>
                </div>
            </div>
            
            {% if tables.len() > 0 %}
            <div class="section">
                <h2>📊 Detailed Reports</h2>
                {% for table in tables %}
                {% let (title, content) = table %}
                <h3 style="margin-top: 30px; margin-bottom: 15px; color: #667eea;">{{ title }}</h3>
                <div class="table-container">
                    <pre>{{ content }}</pre>
                </div>
                {% endfor %}
            </div>
            {% endif %}
        </div>
        
        <div class="footer">
            Generated by WiseTrader Bot • {{ created_at }}
        </div>
    </div>
</body>
</html>
